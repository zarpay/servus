# Shape follows machina/.github/dependabot.yml. One all-patterns group per job
# type per block — version updates and security updates — so a week with
# several advisories yields ~1 PR per ecosystem rather than one per gem.
#
# Cooldown (3/7/30) lets a release sit before we pull it, so a compromised
# publish has time to be yanked. Version updates only; security updates ignore
# cooldown by design.
#
# Note: Dependabot security updates only touch *direct* dependencies
# (`update-subdependencies: false`). Most of this repo's advisories are on
# indirect gems, so they surface as alerts that no PR will ever fix — they
# need a periodic `bundle lock --update=<gem>` by hand. See FIZZY-3597.
version: 2
updates:
  - package-ecosystem: bundler
    directory: /gem
    schedule:
      interval: weekly
    cooldown:
      default-days: 3
      semver-minor-days: 7
      semver-major-days: 30
    groups:
      gem-dependencies:
        patterns:
          - "*"
      gem-security:
        applies-to: security-updates
        patterns:
          - "*"

  # The dummy Rails app that hosts the specs. Nothing runs it — not CI, the
  # Rakefile, the gem's Gemfile, or the specs — so routine version drift here
  # exercises nothing, and a bump to its Rails pin drags the published gemspec
  # along with it. Limit 0 disables *version* updates; security updates are
  # exempt from the limit, so advisories against this lockfile still get PRs.
  - package-ecosystem: bundler
    directory: /gem/spec/dummy
    open-pull-requests-limit: 0
    schedule:
      interval: weekly
    cooldown:
      default-days: 3
      semver-minor-days: 7
      semver-major-days: 30
    groups:
      dummy-dependencies:
        patterns:
          - "*"
      dummy-security:
        applies-to: security-updates
        patterns:
          - "*"

  - package-ecosystem: npm
    directory: /site
    schedule:
      interval: weekly
    cooldown:
      default-days: 3
      semver-minor-days: 7
      semver-major-days: 30
    groups:
      site-dependencies:
        patterns:
          - "*"
      site-security:
        applies-to: security-updates
        patterns:
          - "*"

  # Actions run with repo secrets, so the cooldown matters most here.
  # Flat window only — github-actions rejects the semver tiers used above,
  # and one unsupported key invalidates the entire file.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    cooldown:
      default-days: 7
    groups:
      actions-dependencies:
        patterns:
          - "*"
      actions-security:
        applies-to: security-updates
        patterns:
          - "*"
